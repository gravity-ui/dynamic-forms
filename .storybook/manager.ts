import {addons} from '@storybook/manager-api';

addons.setConfig({
    bottomPanelHeight: 0,
    rightPanelWidth: 0,
    showTabs: false,
    layout: {
        bottomPanelHeight: 0,
        rightPanelWidth: 0,
    },
});
