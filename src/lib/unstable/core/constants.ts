export enum SchemaRendererMode {
    Form = 'form',
    Overview = 'overview',
}

export enum JsonSchemaType {
    Any = 'any',
    Array = 'array',
    Boolean = 'boolean',
    Number = 'number',
    Object = 'object',
    String = 'string',
}

export const EMPTY_OBJECT = {};
